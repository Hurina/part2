import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

const course = (props) => {
  const {course} = props 
  return (
    <div>
                   
    </div>
  )
}

  ReactDOM.render(
    <App course={course} />,
    document.getElementById('root')
  )